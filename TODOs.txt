TODOs for later

1. Remove the remaining legacy aliases from the domain surface.
   - Keep `id_judoka`, `id_competition`, `id_combat`, `prenom`, `nom`, `resultat` only as persistence-compatible adapter fields.
   - Prefer domain names such as `judokaId`, `competitionId`, `combatId`, `PersonName`, `CompetitionDraft`, `CombatDraft`.

2. Split read concerns from write concerns more explicitly.
   - Introduce dedicated read models for profile, competition detail, and competition lists.
   - Keep aggregates focused on write-side invariants and behavior.

3. Enrich the remaining value objects.
   - Add richer types for competition date, age category, weight category, ranking, and combat result if the next refactor needs stronger guarantees.
   - Keep email and name normalization in domain objects, not services.

4. Move any remaining orchestration-only rules out of services when they become stable.
   - Examples: access resolution, competition ownership checks, child removal decisions.
   - Services should mostly load data, call the domain, and persist the result.

5. Revisit the UI and structure tests once the legacy aliases disappear.
   - Update assertions to target the richer domain language instead of compatibility fields.
   - Remove tests that only lock old implementation details.

6. Decide whether `Competition` and `Combat` should become explicit aggregate roots or stay as lightweight behavior-rich entities.
   - If the domain keeps growing, prefer methods like `recordCombat`, `changeDetails`, and `changeResult` over service-side branching.
